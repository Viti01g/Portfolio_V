# GitHub API Token (Opcional pero recomendado)
# Sin token: 60 peticiones/hora
# Con token: 5000 peticiones/hora
# 
# Para crear un token:
# 1. Ve a https://github.com/settings/tokens
# 2. Click en "Generate new token (classic)"
# 3. Marca el scope "public_repo"
# 4. Copia el token y pégalo aquí
# 5. Renombra este archivo a .env
#
# VITE_GITHUB_TOKEN=tu_token_aqui
